// Obtener referencia a los elementos HTML
const locationElement = document.getElementById('location');

// Obtener ubicación actual utilizando AJAX o solicitudes a la API de geolocalización
// Aquí solo se muestra un ejemplo estático
const defaultLocation = 'Lisboa';
locationElement.textContent = defaultLocation;
